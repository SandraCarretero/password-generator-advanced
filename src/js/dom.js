// Aquí van los getElementById y los querySelectorAll que pondrías al principio.

const passwordElement = document.getElementById('password');
const passwordLengthElement = document.getElementById('password-length');
const rangeElement = document.getElementById('range');
const buttonElement = document.getElementById('button-generate');
const optionsElement = document.getElementById('options');

export {passwordElement, passwordLengthElement, rangeElement, buttonElement, optionsElement}